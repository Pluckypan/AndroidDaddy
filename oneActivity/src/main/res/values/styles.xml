<resources>

    <!-- Application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

    </style>

    <!-- Host activity theme. -->
    <style name="AppTheme.HostActivity">
        <item name="android:windowBackground">@android:color/background_dark</item>
    </style>

    <declare-styleable name="DonFrameLayout">
        <attr name="radius" format="dimension|reference" />
        <attr name="left_top_radius" format="dimension|reference" />
        <attr name="right_top_radius" format="dimension|reference" />
        <attr name="right_bottom_radius" format="dimension|reference" />
        <attr name="left_bottom_radius" format="dimension|reference" />
    </declare-styleable>

    <declare-styleable name="DonProgressView">
        <attr name="stroke_width" format="dimension|reference" />
        <attr name="stroke_color" format="color|reference" />
        <attr name="progress_color" format="color|reference" />
        <attr name="type" format="enum">
            <enum name="TYPE_STROKE" value="0" />
            <enum name="TYPE_FILL" value="1" />
        </attr>
        <attr name="progress" format="integer" />
    </declare-styleable>

    <!--状态类型：Toast Loading Progress 对应的消息样式-->
    <style name="DonStatusMessageStyle">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:minWidth">65dp</item>
    </style>

    <!--状态类型：Toast Loading Progress 对应不同View的样式-->
    <style name="DonStatusViewStyle">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>

    <!--状态类型：Toast Loading Progress RootView的样式-->
    <style name="DonStatusRootViewStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:background">#88000000</item>
        <item name="android:gravity">center</item>
        <item name="android:orientation">vertical</item>
        <item name="android:padding">10dp</item>
    </style>

</resources>
